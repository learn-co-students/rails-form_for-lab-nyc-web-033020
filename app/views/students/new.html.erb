<h1>Student Form</h1><br>
<%= form_for @student do |f| %>
<%= f.label 'First name:'%><br>
<%= f.text_area :first_name %><br>

<%= f.label 'Last name:'%><br>
<%= f.text_area :last_name %><br>

<%= f.submit "Create Student"%><br>
<%end%>